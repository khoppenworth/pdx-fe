(function() {
    'use strict';

    angular.module('pdx.factories')
        .factory('ResourcesFactory', function($resource, AppConst, CommonService) {
            var resources = {};

            return resources;


        });
})();